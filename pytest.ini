[pytest]
# Se recogen tanto las pruebas de tests/ como los ejemplos (doctests) incluidos
# en las docstrings del paquete dinamica.
testpaths = tests dinamica
addopts = -q --doctest-modules
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
